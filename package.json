{
  "scripts": {
    "start": "vite",
    "host": "vite --host",
    "build": "vite build",
    "preview": "vite build && vite preview",
    "deploy": "vite build --base=/final-prototype/ && gh-pages -d dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/FrannieEllo/final-prototype.git"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "vite": "^4.1.4"
  },
  "dependencies": {
    "lit-html": "^2.6.1"
  }
}